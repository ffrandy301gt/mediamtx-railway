server:
  # Usa el puerto 80 porque Render expone ese puerto para HTTP
  httpAddress: :80

rtmp:
  enabled: yes

hls:
  enabled: yes
  alwaysRemux: yes

webrtc:
  enabled: yes
  iceServers:
    - urls: [stun:stun.l.google.com:19302]

paths:
  all:
    source: publisher

